

1.git checkout . или git checkout + файл

2.git restored --staged + файл

3.git reset HEAD^ - 1 шаг назад ^^ -c2 шага назад
	  HEAD~1- 1 шаг назад ~2 - 2 шага назад


4. git branch + название ветки -- создать новую ветку
   git checkout + название ветки -- переключиться в новую ветку

   git checkout -b -- создать новую ветку и переключиться в нее 

5.fast-forward merge - 

non fast-forfard merge - к, потом git add ., 
git commit -m ""  не быстрая перемотка

6. git add ., git commit -m "",
git push


7.Переходим в главную ветку main,
 и с помощью команды git branch -d +  -- удаляю ветку



